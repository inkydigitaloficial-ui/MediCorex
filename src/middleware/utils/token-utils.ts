
// Este arquivo não é mais necessário, pois a validação do token foi movida
// para `src/utils/session.ts` e é usada diretamente nos Server Components.
// Manter um arquivo vazio para evitar erros de importação caso ainda seja
// referenciado em algum lugar antes da limpeza completa.

export class TokenUtils {
  // A lógica foi movida.
}
